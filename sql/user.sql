select version();

-- 创建数据库
-- create database hoa default character set utf8mb4 collate utf8mb4_bin;
-- use hoa;

-- 用户表
-- create table if not exists user (id integer not null, firstname varchar(50), lastname varchar(50), age integer, primary key (id)) engine=InnoDB

-- 插入数据
-- select max(id) from user;
-- insert into user (firstname, lastname, age, id) values (?, ?, ?, ?)